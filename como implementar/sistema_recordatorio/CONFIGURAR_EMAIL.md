# Configuración de Emails - Sistema de Recordatorio

La guía vigente (Resend + Vercel Cron, sin GitHub Actions) está en:

**[../CONFIGURAR_EMAIL.md](../CONFIGURAR_EMAIL.md)**

SMTP Gmail queda solo como fallback. La variable correcta es `SMTP_PASSWORD` (también se acepta `SMTP_PASS`).
