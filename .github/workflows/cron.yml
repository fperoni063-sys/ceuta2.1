name: Trigger Cron Job

# DISABLED 2026-08-26
# This hourly job never had GitHub secrets APP_URL / CRON_SECRET, so "Check Secrets"
# failed on every run (2,264 failures). GitHub emailed the repo owner each time.
# Recordatorios now run via Vercel Cron once per day (Hobby plan allows daily crons).
# Do not add `schedule:` back unless those secrets exist AND you want hourly runs.

on:
  workflow_dispatch:

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled on purpose
        run: |
          echo "GitHub Actions cron is disabled."
          echo "Scheduled emails are sent by Vercel Cron: /api/cron/send-scheduled-emails"
          echo "See SYSTEM_CONTEXT.md and como implementar/CONFIGURAR_EMAIL.md"
